# PyTorch with CUDA support
# Install with: pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu129
# Note: Use nightly build for sm_120 (RTX 5060 Ti) support

# Transcription and Diarization
faster-whisper>=1.0.0
pyannote.audio>=3.0.0

# Additional utilities
python-docx>=1.1.0
